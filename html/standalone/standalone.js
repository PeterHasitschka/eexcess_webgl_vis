var canvas1, textElm;

var standalone_folder_prefix = "../../";

jQuery(document).ready(function () {
    IQHN.InitHandler.init(jQuery('body'), function () {
        IQHN.Debugger.debug("standalone", "Finished initializing scene. Callback", 3);
    });
});